env: dev
build: dev

service:
  namespace: dev

resources:
  limits:
    memory: 200Mi
    cpu: .2
  requests:
    cpu: .2
    memory: 200Mi
hpa:
  replicas:
    min: 1
    max: 5
  metrics:
    memory: 60
    cpu: 60
